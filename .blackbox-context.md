# 🧠 إعدادات الذكاء الاصطناعي للمشروع (Blackbox Context)

## ⚙️ بيئة المشروع
المشروع مبني باستخدام **React + Supabase**.  
الهدف: إنشاء واجهة ويب تفاعلية تتصل مباشرة بقاعدة بيانات Supabase لإدارة البيانات من خلال دوال (RPC Functions).

---

## 🧩 القواعد العامة
- لا يُسمح بالاتصال المباشر بجداول قاعدة البيانات من React.
- أي عملية (إضافة، تعديل، حذف، استعلام) يجب أن تتم من خلال **دوال Supabase (PL/pgSQL)**.
- React لا يتعامل إلا عبر:
  ```js
  const { data, error } = await supabase.rpc("اسم_الدالة", { المعاملات });
📄 مرجع قاعدة البيانات موجود في الملف:
`database-schema.json`
يحتوي على تعريف كل الجداول والأعمدة والعلاقات.
- **استخدام الـ schema الحقيقية:**  
  أي دالة أو استعلام يجب أن يعتمد على الجداول، الأعمدة، وأنواع البيانات الموجودة فعليًا في قاعدة البيانات (الموجودة في `database-schema.json` أو Supabase).  
  هذا يعني أن أي تعديل على الجداول أو الأعمدة في الـ database سينعكس تلقائيًا دون الحاجة لتعديل الـ Blackbox Context.
